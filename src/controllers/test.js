/* new_pay =  totalPay - deduction
final_pay = new_pay - tax ammount



payroll -> piad history, 
invoices -> kolai kati trina xa

startingdate: user_registered date into days
newpath = public/imgs/userid/days_since_registred%7
if new_path.exists():
    remove_nrew_path()
newpath.save(screnshot) */